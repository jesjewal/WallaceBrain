---
name: User Profile — Wallace
description: Wallace's background, tools, and working style for design/dev work
type: user
---

Wallace works with Adobe InDesign via UXP scripting (Claude Code + MCP plugin "indesign-sidekick"). Design-literate — notices spacing, alignment, typography, and brand consistency. Directs work at a high level ("move the image left so the person is in frame", "too big, shrink those up") and expects Claude to handle all technical details. Works on professional design documents (architecture firm proposals, branded collateral).

Uses Claude Code in terminal. Works on macOS. External SSD labeled "Wallace SSDtorage" is sometimes unmounted — memory symlink may be broken when it's not plugged in.

Obsidian vault is at `~/Documents/GitHub/wallacebrain` — this is a git repo synced to GitHub (jesjewal/WallaceBrain). All notes, daily logs, and project docs should be written here. Never write to `~/Documents/Obsidian/`.
